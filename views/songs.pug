extends layout 

block content 
    h1= title 

    ul 
        each song in song_list 
            li 
                a(href=song.url) #{song.name}
                |  - #{song.artist.name}
                |  (#{song.genre.name})
        else 
            li There are no songs yet! Go add one!

